<%= form_with(model: project, class: "space-y-4", multipart: true) do |f| %>
  <% if project.errors.any? %>
    <div class="bg-red-50 text-red-700 p-4 rounded mb-4">
      <ul>
        <% project.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name, class: "block text-sm font-medium text-gray-700" %>
    <%= f.text_field :name, class: "mt-1 block w-full border border-gray-300 rounded px-3 py-2", required: true %>
  </div>

  <div>
    <%= f.label :description, class: "block text-sm font-medium text-gray-700" %>
    <%= f.text_area :description, rows: 4, class: "mt-1 block w-full border border-gray-300 rounded px-3 py-2" %>
  </div>

  <div>
    <%= f.label :banner_image, class: "block text-sm font-medium text-gray-700" %>
    <% if project.banner_image.attached? %>
      <p class="mt-1 text-xs text-gray-500">Current: <%= project.banner_image.filename %></p>
    <% end %>
    <%= f.file_field :banner_image, accept: "image/png,image/jpeg,image/heic,image/heif,image/webp", class: "mt-1 block w-full" %>
  </div>

  <div>
    <%= f.label :readme_link, class: "block text-sm font-medium text-gray-700" %>
    <%= f.url_field :readme_link, class: "mt-1 block w-full border border-gray-300 rounded px-3 py-2", placeholder: "https://" %>
  </div>

  <div>
    <%= f.label :demo_link, class: "block text-sm font-medium text-gray-700" %>
    <%= f.url_field :demo_link, class: "mt-1 block w-full border border-gray-300 rounded px-3 py-2", placeholder: "https://" %>
  </div>

  <div>
    <%= f.label :repo_link, class: "block text-sm font-medium text-gray-700" %>
    <%= f.url_field :repo_link, class: "mt-1 block w-full border border-gray-300 rounded px-3 py-2", placeholder: "https://" %>
  </div>

  <div>
    <%= f.label :is_unlisted, class: "inline-flex items-center gap-2" do %>
      <%= f.check_box :is_unlisted, class: "rounded border-gray-300" %>
      <span class="text-sm text-gray-700">Unlisted</span>
    <% end %>
  </div>

  <div>
    <%= f.submit class: "bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 cursor-pointer" %>
  </div>
<% end %>
